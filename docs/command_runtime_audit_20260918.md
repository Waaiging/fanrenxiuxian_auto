2026-09-18 运行指令审计

本轮结合实时面板、近 24 小时发送台账、机器人回执和 Mini App 日志，检查四账号、13 个身份的 835 条状态记录、132 项控制键。记录中包含 158 条定时/冷却项、626 条事件/流程/查询项、38 条暂停或关闭项，以及 13 条总开关。统计基准为北京时间 09:52 的面板与 10:01 采集的台账；后续另核对了 10:13 的元婴闭关回执。

群指令台账记录了 527 次自动发送，其中 525 次关联了回执，2 次历史发送缺少匹配回执；Mini App 日志有 1,182 条语义请求记录。回执关联需要进一步区分成功、冷却和条件拒绝。本轮未发现越过已生效暂停开关的自动发送。

| 检查项 | 原因与证据 | 修复后的行为 |
| --- | --- | --- |
| 小号野外历练 | 完整 worker 关闭 router 的后台调度，却没有接管历练循环；四身份保留数天前的记录 | 在小号原有调度器注册同一个历练 worker，共享 transport，按参与设置执行，避免重复启动；每身份每日结果继续合并为一条日志 |
| 副号元婴闭关 | 主魂当前为落云宗，10:13 仍发送元婴宗专属指令并被拒绝 | 发送和排程检查当前宗门；条件不符时等待资料变化，面板解释原因，保留开关与已确认的进行中状态；watchdog 不把等待资格当成卡死 |
| 推演、护持排程显示 | 推演面板读取旧的整链时间；发布暂停后，显示仍可能套用整链冷却 | 推演读取自身冷却；发布暂停时两项显示各自截止时间，发布开启时保留整链显示，兼容道号别名下的控制 |
| 助阵与解咒助手显示 | 助阵需要邀请；解咒面板只读取旧的单一委托状态，遗漏其他委托人的最新回执 | 助阵冷却结束后显示等待邀请；助手优先显示有效委托，否则显示等待委托和最新历史回执 |
| 合气丹抗药性 | 四次拒绝回执给出 21–47 秒等待时间，但原逻辑跳过本次服丹机会 | 保存服务器冷却，在本次闭关仍处于冷却且选项有效时重试；自然冷却已结束就继续正常闭关。重启保留计划，未知回执不盲目补发，成功后只增加一次后续闭关 |
| 巡边超时重发 | 00:38:12 的巡边在 60 秒后重发，两条命令最终关联同一条回复 | 出发前持久化待确认标记；超时或重启后先查询巡边状态，确认进行中就接续，未确认则只重查状态。现有证据不能确定服务端执行了两次 |

其余已核对的短间隔发送中，共历心劫的三次“稳”属于三个阶段；探寻裂缝成功后的额外查询用于获取服务器实际冷却。巡边的两次体力不足均在 9–10 秒后换兽成功。日常闭关模式下，深度闭关入口保留为控制项；观星、宗战、Boss 和邀请类任务依赖事件触发。消息 ID 间隔本身不作为漏消息结论。

验证使用 `tools/run_tests.py` 在不含真实配置、session 和运行态的临时目录执行。新增回归覆盖历练调度接管和动态参与、冷却显示、宗门变化和 watchdog、合气丹冷却重试、巡边超时与重启恢复。发布采用本轮增量应用到线上源码快照，保留本地既有修改与线上参与设置。线上验收观察自然自动执行，不额外触发游戏测试指令。
